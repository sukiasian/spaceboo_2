{
    "configurations": [
        {
            "type": "node",
            "name": "vscode-jest-tests",
            "request": "launch",
            "console": "integratedTerminal",
            "internalConsoleOptions": "neverOpen",
            "program": "${workspaceFolder}/backend",
            "cwd": "${workspaceFolder}/backend",
            "args": ["--no-cache", "--detectOpenHandles", "--watchAll=false"]
        }
    ]
}
